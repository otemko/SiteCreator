// Поместите параметры в этот файл, чтобы перезаписать параметры по умолчанию и пользовательские параметры.
{
    "files.exclude": {
        "{.vscode,bin,Controllers,obj,Properties,typings}": true
        "**/*.{cs,xproj,xproj.user,config}": true: true
    },

    "window.zoomLevel": -1
}